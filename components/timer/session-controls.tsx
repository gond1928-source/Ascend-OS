"use client";
export function SessionControls() {
  return (
    <p className="font-mono text-xs text-ink-500">
      Session controls (language + kind picker while timing) — ships with the timer page.
    </p>
  );
}
