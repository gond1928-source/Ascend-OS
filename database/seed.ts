/**
 * database/seed.ts
 *
 * Placeholder for a FUTURE Prisma/SQLite seed script. Ascend OS is
 * local-first and starts every user with a completely empty state — zero
 * sessions, zero XP, zero streaks — so there is intentionally no seed data
 * to run today. This file exists only so the `seed` npm script has
 * somewhere to point once database/schema.prisma is actually wired up to a
 * real database (see lib/storage/session-store.ts for the current
 * localStorage-backed persistence and its migration path).
 */

async function main() {
  console.log("No seed data — Ascend OS starts from a clean, empty state by design.");
}

main();
