// Seeds the database from data/starter-data.json.
// Stub for now — fill in once PrismaClient is generated against a real DB.
import starterData from "../data/starter-data.json";

async function main() {
  console.log(`Would seed ${starterData.sessions.length} sessions once Prisma is connected.`);
}

main();
